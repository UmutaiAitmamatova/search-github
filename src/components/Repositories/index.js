import React from 'react';
import classes from './Repositories.module.scss';

function Repositories() {
  return (
    <div className={classes.repositories}>
        <p className={classes.title}>mypage</p>
    </div>
  )
}

export default Repositories;